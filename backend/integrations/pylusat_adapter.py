from typing import Optional


def compute_proximity_score(latitude: float, longitude: float) -> Optional[float]:
	"""Placeholder that would leverage pylusat distance/density tools.

	For now, return None to indicate not computed. The aggregator will
	gracefully handle missing factors.
	"""
	return None


